---
trdd-id: 18787976-117e-4117-aa31-1cdb8a7c95f9
title: Full-fit the TOC progressive-discovery contract — restructure ~14 skills + publishing prep
column: dev
created: 2026-06-11T14:30:20+0200
updated: 2026-06-14T03:46:27+0200
current-owner: ai-maestro-webdesign (this session)
assignee: ai-maestro-webdesign
priority: 3
severity: MEDIUM
effort: XL
labels: [cpv, toc-contract, skill-restructure, publishing]
task-type: refactor
parent-trdd: TRDD-6d8ffed6
release-via: publish
delivery: direct-push
target-branch: main
test-requirements: [lint]
audit-requirements: []
review-requirements: [human-review]
runtime-targets: [macos]
relevant-rules: []
blocked-by: [weekly-usage-limit-resets-2026-06-16T10:00+0200]
last-test-result: not-run
implementation-commits: [280bad7, eddc818, 4c0fabf, 0126b81, 8dcf233]
external-refs: ["github.com/Emasoft/claude-plugins-validation/issues/73"]
---

# TRDD-18787976 — Full-fit the CPV TOC progressive-discovery contract (restructure ~14 skills) + publishing prep

## ⏵ STATE — READ THIS FIRST ON RESUME (authoritative; supersedes the body) — 2026-06-14

**CAPACITY: subagent workflows WORK again as of 2026-06-14** (weekly cap lifted early OR the account-switcher rotated to fresh quota — a one-agent probe returned READY, then a 9-skill batch ran 4.7M tokens to completion). The earlier "blocked until Jun 16 ~5pm" no longer holds — run the remaining batches NOW. If a batch dies on a limit again, the harness RL-backoff + the heartbeat `[janitor-resume]` retry.

**Current committed state: `8dcf233` — 13 of 14 skills DONE.** Committed: mermaid-render (280bad7); Batch A's 9 prose-trims (eddc818 + 4c0fabf); design-principles (orchestrator, frontmatter byte-identical, solo); diagram-svg 7135→3257t + ascii-diagrams-reference 8389→4082t (8dcf233, aggressive prose-trim). All <4900t, embeds complete (CPV checks heading-text presence — both the `> [file] ch · ch` and indented `  > ch · ch` formats are valid), content preserved vs 9d6be81 (high absent-line counts = reference-bullet/TOC reformatting + content moved to refs, NOT loss — substance verified to survive), frontmatter byte-identical, independently verified. (Old stashes stash@{0}/{1} = superseded incomplete partials — droppable. diagram-svg's TECH-node-shape-vocabulary.md edit was +8/-0 additive.)

**REMAINING 1 — amw-design-md (the only SPLIT):** body 4999t + 3840t embed burden across 31 refs = irreducible (prose-trim can't fit a 5-domain skill with 31 embeds), so SPLIT into 5 children per the planning JSON: **amw-design-md-spec** (format/templates/13-rules) · **-author** (author/archetypes/i18n) · **-extract** (URL/Tailwind/codebase/Figma) · **-audit** (lint/validate/5-pass/rubric) · **-convert** (V2→V1/companions/DTCG/library/apply). Partition design-md's content + its 31 refs across the 5; each child <5000t WITH its ref-subset TOCs embedded + a narrow frontmatter trigger (split the routing coherently, no gap). MUST ALSO update: CLAUDE.md inventory (1→5), the commands pointing at design-md, and cross-refs in other skills linking design-md. Custom verify — the no-shared-edits check does NOT apply (the split legitimately edits CLAUDE.md/commands/cross-refs). HIGH blast radius (public skill surface 1→5) — focused, careful step. THEN full CPV re-validate (authoritative gate for all 14) → #119 publishing-prep.

**LESSON (no-shared-edits artifact):** the per-skill verify check "no file modified outside this skill's dir" FALSE-FAILS when ≥2 skills restructure concurrently in one tree (each verifier sees the siblings' edits). It blocked ui-ux-reasoning in Batch A (actually clean — committed after manual verify). For concurrent batches: REMOVE check #5 from the verify prompt and cascade-check manually (confirm every `git status` path is within the batch skill dirs) before commit; or run special/solo skills one workflow at a time.

**The problem (verified with CPV's real o200k tokenizer):** CPV's progressive-discovery contract requires every SKILL.md to embed the COMPLETE Table of Contents of every `.md` reference it links. At `9d6be81`, **125 embed-MINORs** live in 11 skills, and **3 skills are over the 5000-token body cap** (MAJOR). The skills were authored at the token ceiling: ~25 skills sit at 4300-4999 body tokens, so the 11 non-compliant ones have no room to embed their refs. (The other ~14 edge-zone skills are ALREADY compliant+fitting — leave them.)

**Scope = 14 skills** (11 embed-MINOR owners + 3 over-cap):
design-md, design-principles, ux-evaluator, infographics, stitch-mcp-integration, html-diagram, ui-ux-reasoning, svg-diagram, diagram-editorial, ui-sound-design, pretext, ascii-diagrams-reference(8389t), diagram-svg(7135t), mermaid-render(5301t).

**NEXT ACTION:** complete the REMAINING 4 (above) — capacity is back, run now. Harness template = the corrected `…/workflows/scripts/toc-fullfit-batcha-prosetrim-wf_88c18184-f86.js` (each agent READS+EXECUTES its pre-decided plan from `planning-proposals.json`; CPV path 2.126.8; RL-backoff recognises session/weekly-limit strings). For the 3 non-split skills, REMOVE the verify's no-shared-edits check (artifact) and cascade-check manually before commit; for design-md run the SPLIT solo with a verify that ALLOWS CLAUDE.md/commands/cross-ref edits. Commit each clean skill immediately. After all 14: full CPV re-validate, then #119 publishing-prep. (mermaid + Batch A's 9 are DONE — skip them.)

### Load-bearing facts / gotchas (cost real time — do NOT relearn)
- **Measure tokens with CPV's real tokenizer, NOT chars/4** (chars/4 under-counts BPE badly; it caused 2 false "fits"). Command:
  `python3 -c "import sys;sys.path.insert(0,'<CPV>/scripts');from cpv_token_estimate import estimate_tokens;from cpv_validation_common import parse_frontmatter;c=open('<SKILL.md>').read();_,b,_=parse_frontmatter(c);print(estimate_tokens(b).tokens, c.count(chr(10))+1)"`. Cap: body `< 5000` tokens AND `< 500` lines. CPV path (current): `~/.claude/plugins/cache/emasoft-plugins/claude-plugins-validation/2.126.8` (token API stable 2.126.1→2.126.8; live values match the planning baseline exactly).
- **The cascade trap:** shrinking a SHARED reference file's TOC invalidates the embedded copy in EVERY skill that links it (~33 skills, not 11). The first workflow did this and blew findings 125→207 + MAJOR 3→6. **Therefore: lead with SPLIT / PROSE-TRIM-into-PRIVATE-refs; do NOT shrink shared refs** (or, if you must, re-embed ALL consumers coherently in one pass). Each skill agent edits only its own files → race-free.
- **Strategy** (user's verbatim spec is now a skill: `~/.claude/skills/skill-toc-budget-fitting/`): (1) make reference-file TOCs less granular (merge content into fewer broad chapters); (2) split a monolithic SKILL.md into smaller focused skills (by domain / phase / output-type / algorithm). HARD INVARIANT: never omit/conceal/remove any TOC entry — content moves under broader headings, never deleted.
- **Throttle workflows:** pool concurrency ≤ 5, spawn ramp ~7-8s, exponential backoff on rate-limit (thrown OR returned-string). A 14-wide burst trips the transient limit (learned: first big run failed all 63 agents instantly).
- **Per-operation independent verification** (user requirement): each restructure is checked by a SEPARATE verifier agent (re-derives fit via CPV tokenizer, complete-embeds, content-preservation vs `git show 9d6be81:<path>`, frontmatter/routing intact, no shared-file edits) with a restructure→verify→retry loop (≤3).
- **Orchestrator caution:** `amw-design-principles` owns the broad design-vocabulary routing (must activate first on generic design intent). The planner found it FITS VIA PROSE-TRIM ONLY (29 refs, 2451t burden) — do NOT split it; prose-trim into private refs and keep its frontmatter `description` byte-identical.

### Decided plans (from `reports/toc-fullfit/planning-proposals.json` — VERIFIED 2026-06-12: ALL 14 have real, detailed plans; the earlier "4 unplanned" note was STALE — the planner finished all 14 at the JSON's 18:04 write, after this block's 14:30 first draft. Each entry has recommended_technique + split_plan OR prose_trim_plan + rationale.)
- **amw-design-md → SPLIT into 5** (body 4999t + 3840t embed burden across 31 refs = irreducible): `amw-design-md-spec` (format/templates/13-rules) · `amw-design-md-author` (author/archetypes/i18n) · `amw-design-md-extract` (URL/Tailwind/codebase/Figma) · `amw-design-md-audit` (lint/validate/5-pass/rubric) · `amw-design-md-convert` (V2→V1/companions/DTCG/library/apply). Reference allocation per child is in the JSON. Update CLAUDE.md inventory + cross-refs + commands that point at design-md.
- **amw-design-principles → PROSE-TRIM only** (fits; do NOT split — orchestrator).
- **amw-ux-evaluator, amw-infographics, amw-stitch-mcp-integration → PROSE-TRIM only** (per JSON).
- Remaining (html-diagram, ui-ux-reasoning, svg-diagram, diagram-editorial, ui-sound-design) → see JSON proposals.
- diagram-svg / pretext / mermaid-render / ascii-diagrams-reference: plans ARE in the JSON (prose-trim each). mermaid-render DONE+committed (280bad7); diagram-svg + ascii-diagrams-reference DEFERRED (batch-2 left them incomplete — see STATE block).

## Publishing-prep directives (user, 2026-06-11) — action at publishing time (post-restructure)

1. **Remove the local validation/publish scripts; only invoke the cpv plugin.** Verified: `scripts/publish.py` (67KB, custom 11-stage pipeline) already invokes CPV remotely via `uvx cpv-remote-validate plugin . --strict` (never vendors a local copy) and `scripts/cpv_network_resilience.py` supports it. The directive = STOP maintaining the bespoke local pipeline and instead invoke the installed **cpv plugin's** own commands/skills (canonical-pipeline, publish-to-marketplace, cpv-batch-validate). This is a careful publishing-infra refactor — verify-first, commit-before-delete (RULE 0). Also audit `.github/workflows/{ci,release,cross-refs}.yml` for local-validation steps to replace with cpv-plugin invocation. The plugin's OWN output validators (`bin/amw-validate-*.{sh,py}`, `amw-ai-slop-check.py`, etc.) are NOT CPV and STAY — they validate the artifacts the plugin produces.
2. **Open many CPV issues reporting all FPs/errors.** Already filed: #73 (skillaudit text-scans a binary DSIG TTF font → CRITICAL INDIRECT_PROMPT_INJECT ×2 + REGEX_DOS ×5). To file: the matcher-precision FP-shapes catalogued in `reports/cpv-scan/20260609_235732+0200-intent-nit-passivation.md` (CSS font-stack→ReDoS; SVG `feMerge`→PROTOTYPE_POLLUTION; PowerShell `$env:`→LOG_INJECTION; markdown `../` links→PATH_TRAVERSAL; static heredoc help-text→CMD_INJECTION/SUPPLY_CHAIN; skill activation-guard & one-way-delegation docs→A2A_CROSS_AGENT_INJECT/A2A_CAPABILITY_ABUSE). Cross-project rule: ISSUES only, never PRs to CPV. Every issue body MUST begin with `This is the Claude responsible for the ai-maestro-webdesign project.`
3. **NO-EXEMPT DOCTRINE (policy change).** The exempt/allowlist mechanism is being removed because it was easily exploited by a malicious actor. So: NEVER request CPV add an exemption/allowlist; NEVER use `cpv-fp` suppression comments. For a security finding the ONLY remedies are **devitalize** (rewrite the shape so the scanner proves it inert) or **remove**. Report FPs as matcher-over-match bugs for the maintainer to tighten precision — not as "please exempt X". Never suppress a genuinely-malicious thing. (This tightens the existing CPV-maintainer doctrine; reflect it in `~/.claude/skills/skill-toc-budget-fitting` if security-adjacent, and in any future security-fix work.)

## SUPERSEDED — do NOT carry forward
- ✗ "shrink shared reference TOCs then embed 11 skills" — REVERTED (cascade: 125→207 MINOR, 3→6 MAJOR). Replaced by: split/prose-trim into PRIVATE refs, no shared-ref shrink.
- ✗ chars/4 token estimates — WRONG, under-counts. Use CPV's o200k tokenizer.
- ✗ "~25 skills must be restructured" — over-estimate; the edge-zone skills mostly already fit WITH embeds. Real scope = the 14 listed.
- ✗ the cached shrink workflow (`wf_6e578321-79b`, wf `wf_8ddfde03-ecc`) results — those shrank shared refs (cascade); do not reuse.

## Durable artifacts to read before acting
- `reports/toc-fullfit/planning-proposals.json` — the per-skill restructure proposals (technique + child partition + ref allocation).
- `reports/cpv-scan/20260611_111655+0200-toc-postlinkfix-final.log` — the 9d6be81-state full scan (125 embed MINORs, the finding lines).
- `reports/cpv-scan/20260609_235732+0200-intent-nit-passivation.md` — the catalogued security FP-shapes (for the CPV issues).
- `~/.claude/skills/skill-toc-budget-fitting/SKILL.md` — the user's verbatim 2-method spec + operational playbook.
- `scripts_dev/measure_skill_tokens.py` — accurate per-skill body-token measurement via CPV tokenizer.

## Body — full context

(See the STATE block above — it is authoritative. The committed CPV-fix history is commits 492259b → 9d6be81; this TRDD covers the remaining TOC-embed-contract full-fit + publishing prep, blocked on the weekly limit until 2026-06-16.)
