## Table of Contents

- [Optional extension sections](#optional-extension-sections)
- [Validation](#validation)
- [Cross-references](#cross-references)

# Community 9-section DESIGN.md template (Variant 2)

Copy the block below to a new `DESIGN.md`. Fill every `{{placeholder}}` with a real value. Do not leave `TBD`, `???`, or `<TODO>` — the review rubric ([review-rubric](review-rubric.md)) fails any file containing those.
> [review-rubric.md] Output schema · Structural checks (must-pass) · Token-quality checks (must-pass — both variants) · Sync checks (must-pass — when companion files exist) · Content-integrity checks (soft — affects score) · A11y checks (must-pass — both variants) · Scoring · What the rubric does NOT do · How `amw-design-md-author-agent` uses the rubric on its own output · Cross-references

This format dominates the public DESIGN.md corpus (58-brand pre-paywall, 98-brand design-swatches). Use it when matching an existing collection style; otherwise prefer the canonical template.

The XML boundary tags `<context>`, `<design_tokens>`, `<constraints>` are an enhancement that improves quality-rubric scores ([review-rubric](review-rubric.md) S2-S4). Keep them in delivered files.
> [review-rubric.md] Output schema · Structural checks (must-pass) · Token-quality checks (must-pass — both variants) · Sync checks (must-pass — when companion files exist) · Content-integrity checks (soft — affects score) · A11y checks (must-pass — both variants) · Scoring · What the rubric does NOT do · How `amw-design-md-author-agent` uses the rubric on its own output · Cross-references

---

```markdown
# Design System Inspired by {{Brand Name}}

<context>
## Overview

**Organization:** {{Brand or Organization Name}}
**Product:** {{One-sentence product description}}
**Target user:** {{Role plus context — not demographics}}
**Source of truth:** This file. Agents must not override these tokens without a human-approved diff.
**Design-tokens mirror:** `./design-tokens.json` (W3C format, generated by `bin/amw-design-md-emit-companions.py`)
</context>

---

<design_tokens>

## 1. Visual Theme & Atmosphere

{{One to two paragraphs of prose describing the atmosphere, emotional tone, and brand identity. Concrete adjectives, not marketing copy.}}

{{One paragraph describing the typography and spacing philosophy.}}

**Key Characteristics:**
- Accent color: `{{#hex}}` — {{descriptor}}
- Background: {{descriptor}}
- Font family: `{{Font Name}}` at {{px}}/{{weight}} for body, `{{Font Name}}` for display
- Interaction pattern: {{descriptor — e.g., subtle hover lift, no easing>200ms, etc.}}
- Border radius: {{descriptor — e.g., universal 8px, no sharp edges}}
- Spacing rhythm: {{base unit + scale}}
- Design philosophy: {{one-sentence statement}}

---

## 2. Color Palette & Roles

### Primary Brand
- **{{Color Name}}** (`{{#hex}}`): {{Role + CSS variable name if any}}
- **{{Color Name Hover}}** (`{{#hex}}`): Hover state for primary CTAs

### Secondary & Accent
- **{{Color Name}}** (`{{#hex}}`): {{Role}}
- **{{Color Name}}** (`{{#hex}}`): {{Role}}

### Surface & Background
- **Surface Primary** (`{{#hex}}`): Default page background
- **Surface Secondary** (`{{#hex}}`): Card, panel, elevated surface
- **Surface Inverse** (`{{#hex}}`): Dark UI regions, footers

### Neutrals & Text
- **Text Primary** (`{{#hex}}`): Body copy on primary surface
- **Text Secondary** (`{{#hex}}`): Captions, metadata
- **Text Inverse** (`{{#hex}}`): Text on inverse surface
- **Text Link** (`{{#hex}}`): Inline links

### Semantic
- **Success** (`{{#hex}}`): Success states
- **Warning** (`{{#hex}}`): Warning states
- **Danger** (`{{#hex}}`): Error / destructive
- **Info** (`{{#hex}}`): Informational

### Border System
- **Border Subtle** (`{{#hex}}`): Default hairline
- **Border Strong** (`{{#hex}}`): Form field focus, emphasis

---

## 3. Typography Rules

### Font Family

- **Display**: `{{Font Name}}` ({{weight range}}), fallback: `{{system-ui, sans-serif | etc.}}`
- **Body / UI**: `{{Font Name}}` ({{weight range}}), fallbacks: `{{Helvetica, Arial}}`
- **Mono**: `{{Font Name}}`, fallback: `ui-monospace, SFMono-Regular, Menlo, monospace`

### Hierarchy

| Role | Font | Size | Weight | Line Height | Letter Spacing | Notes |
|------|------|------|--------|-------------|----------------|-------|
| Display Hero | {{Family}} | {{px}} ({{rem}}) | {{weight}} | {{lh}} | {{tracking}} | {{notes}} |
| Section Heading (H2) | {{Family}} | {{px}} ({{rem}}) | {{weight}} | {{lh}} | {{tracking}} | |
| Subheading (H3) | {{Family}} | {{px}} ({{rem}}) | {{weight}} | {{lh}} | {{tracking}} | |
| Card Title (H4) | {{Family}} | {{px}} ({{rem}}) | {{weight}} | {{lh}} | {{tracking}} | |
| Body Large | {{Family}} | {{px}} ({{rem}}) | {{weight}} | {{lh}} | {{tracking}} | |
| Body | {{Family}} | {{px}} ({{rem}}) | {{weight}} | {{lh}} | {{tracking}} | |
| Button | {{Family}} | {{px}} ({{rem}}) | {{weight}} | {{lh}} | {{tracking}} | |
| Caption | {{Family}} | {{px}} ({{rem}}) | {{weight}} | {{lh}} | {{tracking}} | |
| Mono / Code | {{Family}} | {{px}} ({{rem}}) | {{weight}} | {{lh}} | {{tracking}} | |

### Principles

- **Tight headings**: Display and H1/H2 use line-height ≤ 1.2.
- **Generous body**: Body copy uses line-height ≥ 1.5 for readability.
- **Tracking**: Negative on display sizes, zero on body, positive on uppercase labels.
- **Weight discipline**: Maximum two weights per screen.

---

## 4. Component Stylings

### Buttons

**Primary**
- Background: `{{#hex}}`
- Text: `{{#hex}}`
- Padding: {{py}} {{px}}
- Radius: {{radius}}
- Font: {{descriptor}}
- Hover: {{description}}
- Active: {{description}}
- Focus: {{description — must include visible focus ring}}
- Use: Single most important action per screen.

**Secondary (Outline)**
- Background: transparent
- Border: 1px solid `{{#hex}}`
- Text: `{{#hex}}`
- Padding: {{py}} {{px}}
- Radius: {{radius}}
- Hover: {{description}}

**Ghost / Tertiary**
- Background: transparent
- Text: `{{#hex}}`
- Hover: subtle bg fill `{{rgba}}`

**Destructive**
- Background: `{{#hex (danger)}}`
- Text: `{{#hex}}`
- Use: Irreversible / data-loss actions only.

### Cards & Containers

**Default Card**
- Background: `{{#hex}}`
- Border: 1px solid `{{#hex}}` or none
- Border radius: {{radius}}
- Padding: {{px}}
- Shadow: {{level 1 shadow formula}}

### Navigation

{{Topbar height, link treatment, active-state indicator, mobile drawer behavior}}

### Inputs & Forms

**Default Input**
- Background: `{{#hex}}`
- Border: 1px solid `{{#hex}}`
- Border radius: {{radius}}
- Padding: {{py}} {{px}}
- Font: {{size}}/{{lh}} {{family}}
- Focus ring: 0 0 0 3px `{{rgba of brand-primary}}`

### Component state diagram

```mermaid
stateDiagram-v2
    [*] --> Default
    Default --> Hover: pointer in
    Hover --> Default: pointer out
    Default --> Focus: keyboard focus
    Focus --> Default: blur
    Default --> Active: press
    Active --> Default: release
    Default --> Disabled: prop
    Disabled --> Default: prop
    Default --> Loading: async action
    Loading --> Default: resolved
    Loading --> Error: failure
    Error --> Default: dismiss
```

---

## 5. Layout Principles

### Spacing System

- **Base unit:** 8px
- **Scale:** 4px, 8px, 12px, 16px, 24px, 32px, 48px, 64px, 96px

### Grid & Container

- Max content width: {{px — typically 1200-1440}}
- Column count (desktop): 12
- Gutter: {{px}}
- Container padding: {{px desktop / px mobile}}

### Whitespace Philosophy

{{2-4 bullets describing the brand's spatial philosophy: generous, sparse, grid-disciplined, etc.}}

### Border Radius Scale

- `xs`: 2px
- `sm`: 4px
- `md`: 8px
- `lg`: 12px
- `xl`: 16px
- `full`: 9999px

---

## 6. Depth & Elevation

| Level | Use | Shadow formula |
|---|---|---|
| 0 | Flush, flat | `none` |
| 1 | Resting card | `{{rgba}} 0px 1px 2px 0px` |
| 2 | Hover card, dropdown | `{{rgba}} 0px 4px 8px -2px, {{rgba}} 0px 2px 4px -2px` |
| 3 | Modal, popover | `{{rgba}} 0px 12px 24px -6px, {{rgba}} 0px 8px 16px -8px` |
| 4 | Toast, overlay top | `{{rgba}} 0px 24px 48px -12px` |

**Z-index scale:** `base: 0, dropdown: 100, sticky: 200, overlay: 300, modal: 400, toast: 500`.

**Rules:**
- Never stack more than one elevation level on the same surface.
- Shadows must use brand-tinted rgba when the brand is strong, else neutral.

---

## 7. Do's and Don'ts

### Do

- Do use semantic token names; never raw hex in component code.
- Do maintain minimum 4.5:1 contrast for body text, 3:1 for large headings.
- Do respect the 8px grid for every spatial value.
- Do use the shadow formulas verbatim.
- Do {{org-specific rule 1}}.

### Don't

- Don't introduce new colors outside this palette. Update this file first.
- Don't use named CSS colors (`red`, `black`); always hex.
- Don't use drop shadows on flat surfaces (body text, inline buttons).
- Don't mix font families within a single component.
- Don't {{org-specific anti-pattern, e.g. "use gradients on CTAs"}}.

---

## 8. Responsive Behavior

### Breakpoints

| Name | Min width | Typical device |
|---|---|---|
| sm  | 640px  | Small phone |
| md  | 768px  | Tablet portrait |
| lg  | 1024px | Tablet landscape / small laptop |
| xl  | 1280px | Desktop |
| 2xl | 1536px | Large desktop |

### Collapsing Strategy

- Typography scales: H1 drops from {{desktop px}} to {{mobile px}} below `md`.
- Gutters shrink from {{desktop px}} to {{mobile px}} below `md`.
- Multi-column layouts collapse to single column below `md`.
- Touch targets must be ≥ 44×44 px on touch devices.
- Hover-only states must have a touch-friendly equivalent.

---

</design_tokens>

<constraints>

## 9. Agent Prompt Guide

### Quick Color Reference
- Primary surface: `{{#hex}}`
- Primary text: `{{#hex}}`
- Brand accent: `{{#hex}}`
- Border: `{{#hex}}`
- Danger: `{{#hex}}`

### Quick Type Reference
- Display: `{{family}} {{size}}/{{weight}}`
- Body: `{{family}} {{size}}/{{weight}}`

### Example Component Prompts
1. "Generate a primary CTA button matching this design system." → Apply Section 4 Buttons / Primary spec.
2. "Render a card containing a title (H4), body text, and a secondary CTA." → Use Card spec from Section 4 + Body spec from Section 3 hierarchy + Secondary button.
3. "Build a sign-in form for the LoginPage." → Inputs default spec from Section 4 + Primary button + 8px stack rhythm from Section 5.

### Iteration Guide

1. Read this entire file before writing any component.
2. Only reference tokens defined here. If a token is missing, stop and ask — do not invent.
3. Match the 9 specs exactly. Visual drift, not variety, is the failure mode.
4. For responsive work, start at the smallest breakpoint and scale up.
5. Keep content on the 8px grid.
6. When in doubt, re-read Section 7 Do's and Don'ts. Don'ts are hard constraints.

**Before shipping:** run the component through the Section 7 checklist. If any line fails, fix it before output.

</constraints>
```

---

## Optional extension sections

Append Sections 10-14 only on explicit user request. See `../extension-sections-10-14.md` for the templates.

## Validation

```bash
# Variant 2 has no canonical linter — use the rubric scorer
python3 <plugin-root>/bin/amw-design-md-validate.py DESIGN.md --variant 2

# Convert to Variant 1 for the official linter check
python3 <plugin-root>/bin/amw-design-md-convert-v2-to-v1.py DESIGN.md DESIGN.v1.md
bash <plugin-root>/bin/amw-design-md-lint.sh DESIGN.v1.md
```

## Cross-references

- [community-9-section-spec](community-9-section-spec.md) — full Variant 2 spec
  > Document head (DESIGN_MD_SPEC L13-L17) · Section count and order (DESIGN_MD_SPEC L25-L36) · No YAML frontmatter · Section specifications · Section 1 — Visual Theme & Atmosphere (DESIGN_MD_SPEC L43-L67) · Section 2 — Color Palette & Roles (DESIGN_MD_SPEC L72-L106) · Section 3 — Typography Rules (DESIGN_MD_SPEC L108-L166) · Section 4 — Component Stylings (DESIGN_MD_SPEC L169-L216) · Section 5 — Layout Principles (DESIGN_MD_SPEC L219-L246) · Section 6 — Depth & Elevation · Section 7 — Do's and Don'ts · Section 8 — Responsive Behavior · Section 9 — Agent Prompt Guide · XML boundary tags (Variant 2 enhancement) · Mermaid component-state diagram · Comparison vs Variant 1 · Cross-references
- [canonical-template](canonical-template.md) — Variant 1 template
  > Filling guide · Cross-references
- [review-rubric](review-rubric.md) — Variant 2 quality scoring
  > Output schema · Structural checks (must-pass) · Variant 1 (canonical) · Variant 2 (community) · Token-quality checks (must-pass — both variants) · Sync checks (must-pass — when companion files exist) · Content-integrity checks (soft — affects score) · A11y checks (must-pass — both variants) · Scoring · What the rubric does NOT do · How `amw-design-md-author-agent` uses the rubric on its own output · Cross-references
- [audit-passes](audit-passes.md) — 5-pass audit
  > Pass 1 — Structural · Pass 2 — Drift · Pass 3 — Accessibility · Pass 4 — Completeness · Pass 5 — Consistency · Output file format · What the auditor does NOT do · Pre-flight checks · Cross-references
- [TECH-13-converting-variant2-to-1](TECH-13-converting-variant2-to-1.md) — V2 → V1 conversion details
  > What it does · When to use · When NOT to use · Conversion overview · Token extraction from V2 prose · Colors · Typography · Spacing · Rounded · Components · Mermaid state diagram · Sections 8-9 merging · Information that may be lost · Inputs · Validation after conversion · Round-trip notes · Cross-references
